const express = require('express'); const cors = require('cors'); const dotenv = require('dotenv'); dotenv.config(); const app = express(); app.use(cors()); app.use(express.json()); app.use('/api/search', require('./routes/search')); app.get('/', (req, res) = res.json({ status: 'ok', service: 'LeadFinder API v2' }); }); const PORT = process.env.PORT 
